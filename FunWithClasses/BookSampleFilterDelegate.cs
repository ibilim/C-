//EIn Delegate ist ein Datentyp der in der Lage ist, eine Methode bzw. eine Menge von gleichartigen Methoden zu kapseln
//Ein Delegate wird als Methodensignature definiert. Der Name der "Methode ist der Name des Delegate-Typs.
//Delegates kapseln methode!!

//Ein Delegate is wie ein Funktionzeiger 
public delegate bool BookSampleFilterDelegate(Buchexemplar sample);
